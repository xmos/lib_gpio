cmake_minimum_required(VERSION 3.21)
include($ENV{XMOS_CMAKE_PATH}/xcommon.cmake)
project(test_output)

set(COMMON_FLAGS -O3 -g -DDEBUG_PRINT_ENABLE=1 -report)

include(${CMAKE_CURRENT_LIST_DIR}/../test_deps.cmake)

set(XMOS_SANDBOX_DIR ${CMAKE_CURRENT_LIST_DIR}/../../..)

set(APP_HW_TARGET XK-EVK-XU316)

set(APP_COMPILER_FLAGS_0_0 ${COMMON_FLAGS} -DTIMESTAMPS=0 -DSUPPLY_PIN_MAP=0)
set(APP_COMPILER_FLAGS_0_1 ${COMMON_FLAGS} -DTIMESTAMPS=0 -DSUPPLY_PIN_MAP=1)
set(APP_COMPILER_FLAGS_1_0 ${COMMON_FLAGS} -DTIMESTAMPS=1 -DSUPPLY_PIN_MAP=0)
set(APP_COMPILER_FLAGS_1_1 ${COMMON_FLAGS} -DTIMESTAMPS=1 -DSUPPLY_PIN_MAP=1)

XMOS_REGISTER_APP()
