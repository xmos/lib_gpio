cmake_minimum_required(VERSION 3.21)
include($ENV{XMOS_CMAKE_PATH}/xcommon.cmake)
project(test_input_events)

set(COMMON_FLAGS -O3 -g -DDEBUG_PRINT_ENABLE=1 -report -DXASSERT_ENABLE_ASSERTIONS=1 -DXASSERT_ENABLE_DEBUG=1)

include(${CMAKE_CURRENT_LIST_DIR}/../test_deps.cmake)

set(XMOS_SANDBOX_DIR ${CMAKE_CURRENT_LIST_DIR}/../../..)

set(APP_HW_TARGET XK-EVK-XU316)

set(APP_COMPILER_FLAGS_0 ${COMMON_FLAGS} -DEVENTS=0)
set(APP_COMPILER_FLAGS_1 ${COMMON_FLAGS} -DEVENTS=1)

XMOS_REGISTER_APP()
